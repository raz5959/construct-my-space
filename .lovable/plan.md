

## Plan: Update Navbar - Remove Button, Center & Enlarge Logo

### Changes to `src/components/Navbar.tsx`:
1. Remove the "להדרכה בחינם" button entirely
2. Change flex layout from `justify-between` to `justify-center` to center the logo
3. Increase logo size from `h-10` to `h-16` (or similar) and add some padding

